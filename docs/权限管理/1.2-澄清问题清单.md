# 澄清问题清单 - 权限管理

## 澄清信息

- 功能清单：docs/权限管理/1.1-功能清单.md
- 澄清日期：2026-08-19
- 澄清人：AI 盘问，人确认

## 问题清单

| # | 涉及功能项 | 类型 | 问题 | AI 建议默认值 | 状态 |
|---|-----------|------|------|--------------|------|
| 1 | #1 登录 / #2 登出 | 歧义 | 登录凭证形态：JWT 无状态，还是 Redis 有状态会话？需求写"token/session，不绑定技术栈" | JWT + Redis：签发 token 存 Redis 关联用户，登出/禁用/重置密码时从 Redis 清除实现即时失效 | ✅ 已确认 |
| 2 | #1 登录 | 歧义 | admin 初始账号的密码是什么？"首次登录强制修改初始密码"如何触发？ | 初始密码 `admin123`；sys_user 增加 `force_change` 标记（1=强制改密），登录响应携带该标记由前端跳转改密页 | ✅ 已确认 |
| 3 | #4 #5 用户管理 | 歧义 | 新增用户的"初始密码"默认值是多少？ | 固定默认 `123456`，管理员新增时填写，不强制首次改密 | ✅ 已确认 |
| 4 | #7 重置密码 | 歧义 | 管理员重置密码后，该用户下次登录是否需强制改密？ | 是：重置后置 `force_change=1`，与首次登录同机制 | ✅ 已确认 |
| 5 | #1 #3 登录/菜单树 | 歧义 | 菜单树返回结构（字段、目录/页面两级如何标识）？ | 树节点含 id/权限名/权限编码/parentId/类型(1目录,2页面)/路由URL/排序；仅返回有权限的节点，空角色返回空数组 | ✅ 已确认 |
| 6 | #16 权限校验 | 歧义 | 每个接口绑定所需权限编码的实现方式？ | 自定义注解 `@RequiresPermission("sys:user:list")` + Spring AOP/拦截器校验；未标注解的 /sys/** 接口默认仅需登录 | ✅ 已确认 |
| 7 | #16 权限校验 | 边界 | 白名单接口范围？ | 仅 `/auth/login` 匿名可访问；`/auth/logout`、`/auth/menus`、`/auth/password` 需登录但不需具体权限编码；`/sys/**` 需登录 + 对应权限编码 | ✅ 已确认 |
| 8 | #8 禁用 / #9 删除 | 边界 | 管理员能否删除/禁用自己？admin 账号能否被禁用/删除？ | 不允许删除/禁用当前登录账号；admin 账号不可删除、不可禁用（与"角色不可删改"一致） | ✅ 已确认 |
| 9 | #12 删除角色 | 边界 | 删除仍被用户引用的角色时，是否需二次确认？ | 无需二次确认，直接级联清除角色-权限、用户-角色关联；受影响用户下次登录菜单自动变少/为空 | ✅ 已确认 |
| 10 | #4 #5 用户管理 | 边界 | 用户名唯一校验是否大小写敏感？ | 大小写不敏感唯一（登录时也按大小写不敏感匹配） | ✅ 已确认 |
| 11 | #6 #13 分配角色/权限 | 边界 | 分配集合为空时的行为？ | 空集合 = 清空该用户全部角色 / 该角色全部权限 | ✅ 已确认 |
| 12 | #1 登录 | 边界 | 登录失败错误提示是否区分"用户名不存在/密码错误/禁用"？需求"不做模糊提示" | 区分三类，返回明确业务码：用户名或密码错误 / 用户被禁用 | ✅ 已确认 |
| 13 | #15 权限数据初始化 | 歧义 | 预置哪些权限数据（目录/页面/权限编码）？ | 预置目录"系统管理" + 页面：用户管理(sys:user:*)、角色管理(sys:role:*)，权限编码按 `sys:user:list`、`sys:user:add`、`sys:user:edit`、`sys:user:delete`、`sys:role:*` 等设计 | ✅ 已确认 |
| 14 | #15 权限数据初始化 | 边界 | 初始化幂等判断依据？ | 以权限表是否已存在预置权限编码为准，已存在则跳过（重复启动不重复插入） | ✅ 已确认 |
| 15 | 全部 | 依赖 | 数据库选型？ | MySQL 8（与本地环境一致） | ✅ 已确认 |
| 16 | 全部 | 依赖 | 是否使用 Redis？ | 是：存登录 token（问题 1 的凭证方案要求） | ✅ 已确认 |
| 17 | #3 菜单树 | 歧义 | 登录/菜单接口是否还需返回按钮级权限编码集合？ | 否：需求明确不做按钮级权限，仅返回菜单树 | ✅ 已确认 |
| 18 | #1 登录 | 入口复杂度 | 登录是否需验证码/失败锁定？ | 否：需求 7 明确非目标 | ✅ 已确认 |

## 决策记录

- 全部 18 项均采用 AI 建议默认值（2026-08-19 人确认"没问题"）
- 关键决策：JWT + Redis 凭证方案、admin 初始密码 admin123 + force_change 标记、新增用户默认密码 123456、大小写不敏感用户名、@RequiresPermission 注解校验、预置"系统管理"目录与用户/角色管理页面

## 完成标准自检

- [x] 每个功能项无未确认的歧义
- [x] 每个功能项的边界用例已列出（合法/非法/边界）
- [x] 依赖完整标注（功能间 + 外部系统：MySQL 8 + Redis）
- [x] 问题清单状态列无"待确认"残留
- [x] 功能清单已更新（边界/风险列 + 依赖完善，见 1.1 更新）
