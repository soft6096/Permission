# Tasks: 权限管理模块（RBAC + 登录）

> 单模块 Spring Boot 工程，包根 `com.permission`。Phase 0 / 0.5 为全局共享（所有功能组依赖），先完成。
> 每功能组按"契约测试（先红）→ 实现（绿）"执行。公共组件必须被使用点调用/继承，禁止复制。

## Phase 0 数据库 + 工程骨架（共享基础设施）

- [ ] T001 创建 Maven 单模块工程骨架（pom.xml + 启动类 + application.yml）
      - 文件：`pom.xml`、`src/main/java/com/permission/PermissionApplication.java`、`src/main/resources/application.yml`、`application-dev.yml`、`application-test.yml`
      - 验收：`mvn compile` 通过；pom 依赖对照《2.1-项目约束》技术架构表（Spring Boot 3.2.5 / MP 3.5.7 / jjwt 0.12.5 / BCrypt）
- [ ] T002 建表 SQL（5 张表，按 3.3/3.4 方案 DDL，含唯一索引）
      - 文件：`docs/sql/schema.sql`
      - 验收：在 MySQL 执行 DDL 成功，`SHOW CREATE TABLE sys_user` 与方案一致
- [ ] T003 实体类 + Mapper 接口（简单查询走 BaseMapper/Wrapper，无 XML）
      - 文件：`src/main/java/com/permission/domain/SysUser.java`、`SysRole.java`、`SysPermission.java`、`SysUserRole.java`、`SysRolePermission.java` + `src/main/java/com/permission/mapper/*Mapper.java`
      - 验收：`mvn compile` 通过；实体 @TableName 对应 5 张表，逻辑删除 @TableLogic
- [ ] T004 基础返回结构：R\<T\> / PageResult\<T\> / ResultCode / ServiceException / GlobalExceptionHandler / MybatisPlusConfig（分页插件）
      - 文件：`src/main/java/com/permission/common/web/*.java`、`common/exception/ServiceException.java`、`config/MybatisPlusConfig.java`
      - 验收：`mvn compile` 通过；业务码枚举与约束 #7 完全一致

## Phase 0.5 公共组件（★ 技术方案"公共组件识别"内容，先写契约测试先红 → 实现变绿）

- [ ] T005 JwtUtil + PasswordUtil + 契约测试
      - 文件：`src/main/java/com/permission/common/util/JwtUtil.java`、`PasswordUtil.java` + `src/test/java/com/permission/common/util/JwtUtilTest.java`、`PasswordUtilTest.java`
      - 验收：测试先红（类不存在编译失败）→ 实现后绿；JWT 签发解析往返正确，BCrypt matches 正确
- [ ] T006 RedisTokenStore + 契约测试
      - 文件：`src/main/java/com/permission/common/util/RedisTokenStore.java` + `src/test/java/com/permission/common/util/RedisTokenStoreTest.java`
      - 验收：save/remove/removeByUserId 语义正确；测试用嵌入式 Redis 或 mock RedisTemplate（test-standards 决定）
- [ ] T007 MenuTreeBuilder + 契约测试
      - 文件：`src/main/java/com/permission/common/util/MenuTreeBuilder.java` + `src/test/java/com/permission/common/util/MenuTreeBuilderTest.java`
      - 验收：buildTree（并集去重 + 排序 + 树结构）与 buildFullTree 正确；登录菜单、/auth/menus、权限树三处复用
- [ ] T008 SecurityUtils + AdminGuard + 契约测试
      - 文件：`src/main/java/com/permission/common/util/SecurityUtils.java`、`AdminGuard.java` + 对应测试
      - 验收：ThreadLocal 存取正确；admin 保护（角色编码 admin / 用户名 admin）判定正确
- [ ] T009 AbstractRelService 抽象基类（关联表先删后插）+ 契约测试
      - 文件：`src/main/java/com/permission/common/base/AbstractRelService.java` + 测试
      - 验收：replace 模板正确；UserService.assignRoles 与 RoleService.assignPermissions 必须继承复用

## Phase 1 契约测试（★ 最先写，先红）

- [ ] T010 认证模块接口契约测试（4.1 方案 14 个场景）
      - 文件：`src/test/java/com/permission/controller/AuthControllerTest.java`
      - 验收：运行**全红**（接口未实现，404/编译失败）
- [ ] T011 用户管理接口契约测试（4.2 方案 20 个场景）
      - 文件：`src/test/java/com/permission/controller/UserControllerTest.java`
      - 验收：运行**全红**
- [ ] T012 角色管理接口契约测试（4.3 方案 16 个场景）
      - 文件：`src/test/java/com/permission/controller/RoleControllerTest.java`
      - 验收：运行**全红**
- [ ] T013 权限数据初始化 + 后端权限校验逻辑测试
      - 文件：`src/test/java/com/permission/init/PermissionDataInitializerTest.java`、`src/test/java/com/permission/config/AuthInterceptorTest.java`
      - 验收：运行**全红**

## 功能组 A：认证模块（Phase 2→5）

- [ ] T014 [P] AuthService 接口 + 实现（login/logout/getCurrentUserMenus/changePassword）
      - 文件：`src/main/java/com/permission/service/AuthService.java`、`service/impl/AuthServiceImpl.java`
      - 验收：菜单树并集去重正确（复用 T007 MenuTreeBuilder）、登出幂等、改密后清全部 token（复用 T006）
- [ ] T015 [P] AuthController（4 接口，白名单）
      - 文件：`src/main/java/com/permission/controller/AuthController.java`
      - 验收：T010 变绿；登录返回 token+forceChange+menus
- [ ] T016 quickstart 认证链路
      - 验收：`curl -X POST /auth/login` 返回 200 + token；带 token 调 /auth/menus 返回菜单树；登出后旧 token 401

## 功能组 B：用户管理（Phase 2→5）

- [ ] T017 [P] UserService 接口 + 实现（8 方法，含 admin 保护/级联删除/重置禁用强制下线）
      - 文件：`src/main/java/com/permission/service/UserService.java`、`service/impl/UserServiceImpl.java`
      - 验收：用户名唯一校验（复用 PasswordUtil T005、RedisTokenStore T006、SecurityUtils T008、AdminGuard T008）
- [ ] T018 [P] UserController（8 接口，@RequiresPermission）
      - 文件：`src/main/java/com/permission/controller/UserController.java`
      - 验收：T011 变绿；权限码 sys:user:query/add/edit/delete
- [ ] T019 quickstart 用户管理链路
      - 验收：curl 增删改查 + 分配角色走通；admin 保护返回 1006

## 功能组 C：角色管理（Phase 2→5）

- [ ] T020 [P] RoleService 接口 + 实现（7 方法，含 admin 角色保护/级联删除）
      - 文件：`src/main/java/com/permission/service/RoleService.java`、`service/impl/RoleServiceImpl.java`
      - 验收：roleCode 唯一校验、分配权限继承 AbstractRelService（T009）、权限树复用 MenuTreeBuilder（T007）
- [ ] T021 [P] RoleController（7 接口，@RequiresPermission）
      - 文件：`src/main/java/com/permission/controller/RoleController.java`
      - 验收：T012 变绿；权限码 sys:role:query/add/edit/delete
- [ ] T022 quickstart 角色管理链路
      - 验收：curl 增删改 + 分配权限走通；admin 角色保护返回 1006

## 功能组 D：权限数据初始化 + 后端权限校验（Phase 2→5）

- [ ] T023 [P] PermissionDataInitializer（CommandLineRunner，幂等）
      - 文件：`src/main/java/com/permission/init/PermissionDataInitializer.java`
      - 验收：T013 初始化部分变绿；重复启动不重复插入；admin 账号 admin/admin123 forceChange=1
- [ ] T024 [P] @RequiresPermission 注解 + AuthInterceptor + WebMvcConfig（注册拦截器）
      - 文件：`src/main/java/com/permission/common/web/RequiresPermission.java`、`config/AuthInterceptor.java`、`config/WebMvcConfig.java`
      - 验收：T013 拦截器部分变绿；401/403/1007 判定正确；白名单仅 /auth/login

## Phase 4 总体验证

- [ ] T025 全链路 quickstart（启动应用，curl 按"登录→用户管理→角色管理→分配权限→改密"走通）
      - 验收：每个环节返回预期业务码；admin 首次登录返回 forceChange=true，改密后 forceChange=false
- [ ] T026 `mvn test` 全绿 + `mvn package -DskipTests` 可打包
      - 验收：所有契约测试 + 单元测试通过

## Phase 5 收尾

- [ ] T027 注释、事务、权限注解核对（对照 2.1 约束 #5/#12/#15/#16/#17/#18 + comment-standards）
      - 验收：类/方法/字段全注释；多表写 @Transactional 在 Service；/sys/** 全 @RequiresPermission；无 System.out
- [ ] T028 重复代码核对（公共组件复用点检查）
      - 验收：无 ≥2 处相同方法体；T005~T009 组件均被使用点调用/继承，无复制副本

## 完成标准自检

- [x] 每个任务有文件路径 + 验收方法（AI 可独立执行）
- [x] 技术方案验收场景全部翻译成测试任务（T010~T013 覆盖三态场景）
- [x] 技术方案"公共组件识别"已翻译成 Phase 0.5 任务（T005~T009）
- [x] 功能任务中公共组件复用点已写死（T014~T024 标注调用/继承）
- [x] 依赖顺序正确（Phase 0/0.5 完成后才进 Phase 2）
